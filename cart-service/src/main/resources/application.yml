server:
  port: 8081

spring:
  application:
    name: cart-service

  datasource:
    url: jdbc:mysql://localhost:3306/find_buy?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: root
    password: 1176
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

product:
  service:
    url: http://localhost:8080
