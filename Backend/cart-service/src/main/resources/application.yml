server:
  port: 8082

spring:
  application:
    name: cart-service

  datasource:
    url: jdbc:mysql://localhost:3306/ev_trading_cart?useSSL=false&serverTimezone=Asia/Ho_Chi_Minh
    username: root
    password: 0123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect

product:
  service:
    url: http://localhost:8080
